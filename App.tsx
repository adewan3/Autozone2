import React from 'react';
import App from './src/App'; // Import the App component from the src directory

// Export the App component from the src directory as the root component
export default App;